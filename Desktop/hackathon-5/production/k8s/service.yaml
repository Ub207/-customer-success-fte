apiVersion: v1
kind: Service
metadata:
  name: fte-api-service
  namespace: fte-production
  labels:
    app: fte-api
spec:
  selector:
    app: fte-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
  type: ClusterIP
